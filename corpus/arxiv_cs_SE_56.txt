title athena startup kit authors w l p lavrijsen abstract athena startup kit ask interactive front end atlas software framework athena written python effective glue language build top principle unrelated code repository build configuration debug binding analysis tools ask automates many error prone tasks otherwise left end user thereby pre empting whole category potential problems existing tools ask setup user needed locates available resources maintains job coherency manages run time environment allows interactivity debugging provides standalone execution scripts end user wants run analysis algorithms within standard environment let ask generate appropriate skeleton package needed dependencies run time well default job options script new casual users ask comes graphical user interface advanced users ask scriptable command line interface built top set libraries ask need experiment neutral thus built workarounds known gotcha would otherwise major time sink every new user ask minimizes overhead physicists atlas want write run analysis code